import { connect } from 'react-redux';
import ClientTemplate from '../views/template';

const mapStateToProps = (state) => {
    return {};
}

export default connect(mapStateToProps, {

})(ClientTemplate)
